[alias]
xtask = "run -p radroots_sdk_xtask --"

# Same-repository facade overrides for local workspace development.
[patch.crates-io]
radroots_sdk = { path = "crates/sdk" }
radroots = { path = "crates/radroots" }
